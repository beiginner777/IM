# Docker 构建上下文忽略文件
# 关键：build/ 里的 CMakeCache.txt 是宿主机绝对路径（如 /root/IM/linux-server/...），
# 若被 COPY . . 带进镜像，容器内 cmake 会因路径不匹配而报错

# ---- CMake / 编译产物 ----
build/
bin/
CMakeCache.txt
CMakeFiles/
*.o
*.a
*.so

# ---- 运行日志 / 临时文件 ----
logs/
*.log

# ---- 非构建必要内容 ----
.git/
架构图/
